#!/bin/bash

## CORE
INVOICE_PLANE_FQDN=${INVOICE_PLANE_FQDN:-localhost}
INVOICE_PLANE_TIMEZONE=${INVOICE_PLANE_TIMEZONE:-UTC}

## DATABASE
DB_HOST=${DB_HOST:-}
DB_PORT=${DB_PORT:-}
DB_NAME=${DB_NAME:-}
DB_USER=${DB_USER:-}
DB_PASS=${DB_PASS:-}
